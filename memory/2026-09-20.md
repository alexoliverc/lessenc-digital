# Registro operacional — 20/09/2026

## Objetivo e baseline

Execução integral autorizada da P15 — Observability & Operational Readiness, P15-01 a P15-08, na
branch `phase/p15-observability-operational-readiness`, a partir do parent canônico
`ec12362c21682516f4882c7d70fac51974f17072`, com parada obrigatória antes de push, PR, merge, tag,
deploy ou P16.

O checkpoint anotado P14 foi confirmado apontando para
`dfd4977a7c1db00314b613b5d695e166a62d614f`. O worktree inicial estava limpo.

## Implementação

- consolidada a correlação UUID server-authoritative e o contrato de headers interno/público;
- protegido o envelope do logger com schema, serviço, ambiente e campos reservados;
- migrados logs operacionais diretos em `src/` para o logger canônico;
- adicionada telemetria provider-neutral com métricas low-cardinality e spans locais;
- preservado `/api/health` como liveness mínimo e criado `/api/readiness` sanitizado;
- adicionada sonda real para contrato da aplicação, MySQL e storage privado;
- preservado e estendido o health operacional P11 com aliases P15;
- consolidado o modelo de alertas P11, severidade, owner, dedup, SLI e service health;
- criada projeção pública sanitizada para Website, Checkout, Payments, Buyer Access / Digital
  Delivery e Admin;
- documentadas política de backup/recovery, propostas de RPO/RTO/retenção com aprovação do owner
  pendente e validação hosted P16, incident lifecycle,
  nove runbooks e separação rollback/recovery;
- criados arquitetura P15 e dossiê candidato do Gate C;
- nenhum vendor/dependência, tabela, schema ou migration foi adicionado.

## Validação

- R1 targeted: 12 arquivos / 51 testes PASS;
- R1 admin MySQL targeted: 2 arquivos / 14 testes PASS;
- unit/application/P15: 87 arquivos / 721 testes PASS;
- MySQL isolado `127.0.0.1:3307/lessenc_test`: 22 arquivos / 191 testes PASS;
- readiness real focal: 1/1 PASS;
- lint: PASS;
- typecheck: PASS;
- Prisma generate/validate: PASS;
- build otimizado Next.js 16.3.4: PASS, com `/api/health` e `/api/readiness`;
- `npm audit --audit-level=high`: 0 vulnerabilidades;
- health operacional: `OK`, 5/5 checks `OK`, com `DB_RUNTIME_URL` explicitamente igual ao
  `TEST_DATABASE_URL`; uma execução anterior com alvo lógico divergente falhou fechada como
  esperado e não foi tratada como indisponibilidade confirmada;
- bundle P11: verification PASS; restore validation read-only PASS; nenhum restore físico executado;
- smoke otimizado: liveness 200 `{"status":"ok"}` e readiness 200 `{"status":"ready"}`, ambos
  com correlação válida; readiness `no-store` e política global P14 de referrer preservada;
- processos do smoke encerrados, porta 3105 livre e diretórios temporários removidos.

Uma execução concorrente de unit tests e lint fez o teste de lazy import administrativo exceder o
timeout de cinco segundos. A suíte R1 completa passou isolada em 87/87 arquivos e 721/721 testes; o
sinal foi classificado como contenção local não reproduzida, não
como regressão funcional.

## Estado e pendências

P15 está `COMPLETE / PASS / DOCUMENTED / AWAITING GIT INTEGRATION`.
Gate C ainda não foi concedido. Não houve commit, push, PR, merge, tag, checkpoint ou deploy; `main`
não foi alterada e P16 não foi iniciada.

P16 permanece responsável por staging/Hostinger, DNS/TLS, storage/DB/secrets hosted, controle de
exposição machine/internal de `/api/readiness`, monitor externo, status page real, entrega de
alertas, scheduling/encryption/off-site de backup, aprovação/medição hosted das propostas RPO/RTO/
retenção e rollback hosted. P17 permanece responsável por E2E real de browser/provider.

## P15 R1 — correções pós-auditoria independente

- checkout e quatro superfícies administrativas passaram a gerar correlação pelo helper P15, sem
  substituir IDs de Order/Item/Customer/eventos/entidades;
- readiness propaga a correlação do request até probe/log/metric/span/response; chamada não HTTP
  ainda gera UUID canônico novo;
- 36 arquivos exatos reportados pelo Prettier foram normalizados para tornar o gate global verde;
- RPO `<= 24h`, RTO `<= 8h` e retenção 7/4/3 foram classificados como
  `OWNER APPROVED RELEASE OBJECTIVE / HOSTED VALIDATION REQUIRED IN P16`;
- `/api/readiness` foi formalizado como contrato machine/internal sanitizado, com controle de
  exposição hosted obrigatório em P16;
- operational health passou a importar exports reais de `observability/contracts.ts`, removendo o
  source-text `.includes(...)`.

Validação R1: 12/51 targeted PASS, 2/14 admin integration PASS, 87/721 unit PASS, 22/191 MySQL PASS,
`npm run check` PASS, format check global PASS, Prisma validate PASS, build otimizado PASS e audit com
0 vulnerabilidades. Git lifecycle permanece inalterado e Gate C continua pendente de ChatGPT
re-review.

## P15 Git lifecycle closeout

This closeout supersedes earlier same-day pre-integration lifecycle status entries; those entries remain preserved as historical evidence.

- implementation commit: `7a7ffd498856fc8bd1a3f2a6773a14c34aa52897`;
- implementation PR: `#43` — MERGED;
- canonical technical merge: `be59d791f81fd5c75a5e39ffebd8aa814ca6368b`;
- permanent checkpoint: `checkpoint/p15-observability-operational-readiness-complete` -> `be59d791f81fd5c75a5e39ffebd8aa814ca6368b`;
- Gate C — OPERATIONS READY: PASS;
- checkpoint must not be retargeted by the documentation closeout;
- deployment: NOT PERFORMED;
- P16: NOT STARTED.

## P16 internal repository implementation

The owner authorized autonomous repository-side P16 work on
`phase/p16-staging-deployment`, starting at
`11c4a18e4c2b479ebd8155f8a5397e8820e49513`, with existing uncommitted P16-02-B changes preserved.
No push, PR, merge, tag, release, deploy or real provider action was authorized or performed.

Implemented internally:

- corrected the environment contract with `P11_BUYER_SESSION_SECRET`, protected-readiness token,
  explicit storage driver, staging identity and release metadata names;
- declared Node.js `24.x` in package metadata and lockfile;
- created a fail-closed staging configuration preflight and migration guard before
  `prisma migrate deploy`, preserving distinct migration/runtime DB users and TLS;
- preserved `PrivateResourceStorage` as the provider-neutral streaming boundary, prohibited local
  filesystem authority in staging/production and failed closed while provider selection is open;
- protected `/api/readiness` with a server-only bearer token, fixed-length digest timing-safe
  comparison, generic denial, no-store and zero unauthorized dependency probes;
- created hosted health/readiness/header smoke tooling without embedding credentials;
- preserved P10 webhook/payment authority and added TEST-only Mercado Pago configuration checks;
- added read-only retention planning for 7 daily / 4 weekly / 3 monthly recovery points;
- documented reproducible deployment, migration, monitoring, recovery and rollback procedures;
- replaced the flaky 5-second generic limit on the Better Auth cold-import property test with a
  bounded 15-second test timeout after reproducing one cold failure and four warm passes, without
  changing authentication runtime behavior.

P16-09 internal evidence:

- focused payment/webhook/storage/readiness: 7 files / 83 tests PASS;
- complete unit/application: 91 files / 740 tests PASS;
- isolated P06 MySQL `127.0.0.1:3307/lessenc_test`: 22 files / 191 tests PASS;
- lint, typecheck, Prisma generate/validate, global format check and `npm run check`: PASS;
- dependency audit: 0 vulnerabilities;
- optimized Next.js staging build: PASS with synthetic ephemeral configuration and no real DB
  connection;
- configuration preflight and migration guard: synthetic positive PASS; missing-config negative
  paths refused with exit 1; no migration executed;
- 23/23 active configuration variables represented in `.env.example`; exactly one intentional
  public credential name, `NEXT_PUBLIC_MERCADOPAGO_PUBLIC_KEY`;
- Prisma schema/migrations unchanged; package dependencies unchanged;
- private-key and credential scan PASS; `git diff --check` PASS.

Final internal classification:

`P16 INTERNAL REPOSITORY IMPLEMENTATION COMPLETE / HOSTED / EXTERNAL VALIDATION PENDING`.

Local Git traceability:

- implementation commit: `87ac86405776371ccc05750d184095915861d2b9`;
- formatting correction after staged diff audit: `358f070dc96503de95fd88889a0aa471aefdb8da`;
- push, PR, merge, tag, release and deploy: NOT PERFORMED.

Owner/external actions remain: real staging secrets, Hostinger configuration/deploy, hosted DB and
CA/TLS, guarded hosted migration, storage-provider decision/adapter/credentials, Mercado Pago TEST
credentials/webhook/payment, external monitor/alert/status page, backup scheduler/encryption/off-site
copy, hosted restore/RPO/RTO/rollback, live HTTPS/header/smoke evidence and final staging validation.

## P16-HDB-01 — Hostinger managed database adaptation

The owner selected Hostinger Managed MariaDB and explicitly approved a single managed database user
with controlled privilege rotation. The additive adaptation preserves `distinct-users` and adds the
opt-in `hostinger-managed-single-user` access model without changing schema or migrations.

Repository behavior:

- `DATABASE_URL` and `DB_RUNTIME_URL` remain mandatory logical contexts;
- matching usernames are accepted only in explicit Hostinger mode and must carry the same physical
  credential for the same hosted staging target;
- migration guard requires `P16_DATABASE_MIGRATION_WINDOW=enabled`;
- ordinary runtime/preflight requires `disabled`;
- privilege elevation/reduction remains an external Hostinger control-plane action;
- protected staging readiness uses read-only `SHOW GRANTS FOR CURRENT_USER()`;
- `distinct-users` accepts only `SELECT`, `INSERT`, `UPDATE`, `DELETE` on the exact staging schema
  plus global `USAGE`;
- real Hostinger hosted validation proved the managed single-user grant also retains
  `DELETE HISTORY` and `SHOW CREATE ROUTINE`, which hPanel does not expose individually;
- those two exact extras are accepted only by `hostinger-managed-single-user`, with continuous
  fail-closed checks requiring zero `SYSTEM VERSIONED` tables and zero stored routines;
- broad, additional, DDL/administrative, incomplete, foreign, global-DML, grant-option or
  uninterpreted grants continue to fail closed without grant disclosure;
- hosted migration completed successfully for all 8 migrations and subsequent Prisma status
  reported the schema up to date;
- migration window was returned to `disabled`;
- TLS CA, `rejectUnauthorized=true` and hostname verification remain unchanged.

No Hostinger panel/database, real credential, hosted migration, Smith Sterling resource, Mercado
Pago, DNS, deploy or production resource was accessed.

Local validation evidence for this additive delta:

- P16-HDB focused suite: 4 files / 36 tests PASS;
- synthetic runtime preflight: PASS;
- synthetic migration guard with disabled window: expected fail-closed PASS;
- synthetic migration guard with enabled window: PASS; Prisma migration was not invoked;
- `npm run check` with explicit `APP_ENV=test` and `NODE_ENV=test`: lint, typecheck, 92 files /
  767 tests and format check PASS;
- the first unqualified `npm run check` attempt was invalid because `APP_ENV` was absent: 90 files /
  763 tests passed, 2 logger tests and 1 storage suite rejected the missing test environment; it was
  superseded by the explicit-environment PASS above;
- isolated P06 MySQL integration at sanitized target `127.0.0.1:3307/lessenc_test`: 22 files / 191
  tests PASS; the pre-existing container was not started or stopped;
- synthetic staging production build: PASS;
- `npm audit --audit-level=high`: 0 vulnerabilities;
- Prisma schema and all migrations: unchanged from starting HEAD `6a85d1a`;
- `git diff --check`: PASS after removing documentation trailing whitespace.

## P16-HDB-01-FIX01 — Migration release-commit binding

Independent review identified `P16-HDB-F01`: the migration guard validated release SHA syntax but
did not itself prove equality with the checked-out repository `HEAD`. The fix extracts a shared
release-binding helper used independently by preflight and migration guard. It resolves
`git rev-parse HEAD`, validates both identities and requires exact equality. Missing, malformed,
mismatched or unverifiable release identity fails closed before Prisma can run.

Focused tests execute the migration guard script itself with synthetic configuration for correct,
different, missing and malformed release commits. Git lookup failure is covered through an injected
resolver without global process mocking. No migration command is executed by the tests.

FIX01 validation evidence:

- focused P16-HDB regression: 5 files / 41 tests PASS, including 5 new binding cases;
- `npm run check` with explicit `APP_ENV=test` and `NODE_ENV=test`: lint, typecheck, 93 files / 772
  tests and format check PASS;
- `npm audit --audit-level=high`: 0 vulnerabilities;
- synthetic staging production build: PASS;
- isolated MySQL integration: NOT EXECUTED — NOT APPLICABLE TO SCRIPT-ONLY FIX; no database,
  Prisma schema, migration or persistence behavior changed;
- `prisma migrate deploy`: NOT EXECUTED — FORBIDDEN BY FIX01.

## P16-HDB-02 — Prisma migration TLS binding

Independent review identified `P16-HDB-F02`: P16 validated `DB_TLS_CA_FILE`, but the Prisma CLI
datasource still consumed raw `DATABASE_URL` without explicitly binding the CA or strict certificate
verification. The remediation adds a pure helper consumed by `prisma.config.ts` only for staging.

The effective staging URL preserves the synthetic connection identity and non-TLS parameters,
removes all case variants of `sslcert`/`sslaccept`, derives `sslcert` from the absolute CA path
relative to `prisma/`, normalizes Windows separators and forces `sslaccept=strict`. Missing,
relative or cross-volume/unrepresentable CA paths and malformed/non-MySQL URLs fail with generic
codes that do not include credentials or URLs. When `DATABASE_URL` is absent, the deliberately
unreachable configuration-only fallback remains unchanged. Local/test URLs remain unchanged.

No Hostinger connection, real credential, hosted Prisma TLS session or migration was used during
implementation. Hosted Prisma TLS proof remains pending.

P16-HDB-02 validation evidence:

- focused P16-HDB regression: 6 files / 53 tests PASS, including 12 new Prisma TLS-binding cases;
- `npm run check` with explicit `APP_ENV=test` and `NODE_ENV=test`: lint, typecheck, 94 files / 784
  tests and format check PASS;
- the first check attempt stopped at lint because the new `.mjs` helper initially relied on the
  global `URL`; importing `URL` explicitly from `node:url` corrected the static error before the
  successful full rerun;
- staging-config `prisma validate`: PASS with synthetic `.invalid` target, conflicting raw TLS
  parameters and local non-certificate readable fixture used only for config construction;
- staging-config `prisma generate`: PASS, Prisma Client 7.10.0;
- synthetic staging production build: PASS; no database connection;
- `npm audit --audit-level=high`: 0 vulnerabilities;
- secret/private-key/hosted-identifier scan: 9 changed files / 0 findings;
- Prisma schema, existing migrations, dependencies and package lock: unchanged; `git diff --check`
  and final format check PASS;
- isolated MySQL integration: NOT EXECUTED — NOT APPLICABLE; URL construction/config loading is
  covered without a connection and no persistence behavior changed;
- hosted Prisma TLS proof and every Prisma migration command: NOT EXECUTED — FORBIDDEN BY HDB-02.

## P16-HDB-03 — Prisma migration strict system trust

Hosted H2-I-D1 and H2-I-D2 isolated `P16-HDB-F03 — PRISMA EXPLICIT SSLCERT INCOMPATIBLE WITH
HOSTED SCHEMA ENGINE`.

Observed hosted behavior:

- TCP connectivity to Hostinger MariaDB: PASS;
- application MariaDB driver with explicit CA and `rejectUnauthorized=true`: previously PASS;
- Prisma `migrate status` with `sslaccept=strict` and public system trust reached migration-state
  inspection;
- raw relative `sslcert`: `P1001`;
- encoded relative `sslcert`: `P1001`;
- root CA directly inside `prisma/`: `P1001`;
- complete CA bundle directly inside `prisma/`: `P1001`;
- `prisma migrate deploy`: NOT EXECUTED;
- database mutation: NOT PERFORMED.

Canonical P16-HDB-03 trust split:

- application runtime: `DB_TLS_CA_FILE` + explicit CA + `rejectUnauthorized=true`;
- Prisma Migrate: public system trust + exactly one `sslaccept=strict` + no `sslcert`.

Operator-provided case variants of `sslaccept` and `sslcert` are removed before the canonical
staging Prisma datasource is emitted. `accept_invalid_certs` cannot become the effective staging
policy.

Gate 1 local evidence:

- 16/16 focused Prisma TLS tests PASS;
- 52/52 focused P16 database-security regression tests PASS;
- runtime explicit CA contract preserved;
- Prisma schema unchanged;
- migrations unchanged;
- packages unchanged;
- no commit, push, deploy or migration executed.

Final P16-HDB-03 hosted revalidation: PASS.

Gate 3-B evidence:

- real Prisma CLI executed with `APP_ENV=staging`;
- canonical Hostinger hostname reached migration-state inspection;
- raw permissive TLS overrides in the input URL were canonicalized by the staging helper;
- effective migration contract remained `sslaccept=strict` with no `sslcert`;
- the same endpoint addressed by resolved IP was rejected with `P1011` and TLS-class evidence;
- canonical-hostname reconfirmation reached migration-state inspection;
- `prisma migrate status` only;
- `prisma migrate deploy`: NOT EXECUTED;
- database mutation: NOT PERFORMED.

Gate 3-C R2 evidence:

- `.env.local` was temporarily quarantined without reading or printing its contents;
- process environment was sanitized from `.env.example` plus synthetic P16 staging values;
- database targets were synthetic `.invalid` only;
- Next.js 16.3.4 optimized production build: PASS;
- Prisma Client 7.10.0 generation during prebuild: PASS;
- `.env.local` restored successfully;
- repository HEAD unchanged;
- worktree remained exactly the nine P16-HDB-03 files;
- `git diff --check`: PASS;
- no hosted database connection, deploy, migration or database mutation occurred.

## P16-H2-M1 — Hostinger runtime privilege reconciliation

Real hosted H2-L validation established that after hPanel reduction the managed database identity
retains:

- global `USAGE`;
- `SELECT`, `INSERT`, `UPDATE`, `DELETE`;
- provider-managed `DELETE HISTORY`;
- provider-managed `SHOW CREATE ROUTINE`.

`ALL PRIVILEGES` and `GRANT OPTION` are absent. Hosted inspection found zero system-versioned tables
and zero stored routines, so the two Hostinger extras currently have no corresponding target
surface.

H2-M1B updated the readiness contract so `distinct-users` remains strictly four-DML while
`hostinger-managed-single-user` permits only the two exact provider extras. Readiness additionally
queries metadata and returns `DATABASE_RUNTIME_PRIVILEGES_UNSAFE` if a system-versioned table or
stored routine appears.

Focused H2-M1B validation: 4 test files / 42 tests PASS, typecheck PASS, lint PASS and diff check
PASS. No commit, push or application deploy had been performed at the time of this documentation
reconciliation.

<!-- P16-H3-B1-CLOUDFLARE-R2 -->

## P16 H3-B1 — Cloudflare R2 architecture freeze

Owner approved Cloudflare R2 Standard for P16 hosted private storage.

Frozen design:

`PrivateResourceStorage -> S3CompatiblePrivateResourceStorage -> Cloudflare R2 private bucket`

Runtime uses bucket-scoped `Object Read only`; Smith Sterling resources/credentials are isolated
and cannot be reused.

Protected delivery remains backend-proxied. Public `r2.dev`, custom public storage domain, direct
object URLs and presigned buyer URLs are not part of P16.

`stat()` maps to `HeadObject`, `open()` maps to `GetObject`, and hosted readiness will use
`HeadObject` against `_health/p16-readiness`.

H3-B1 is documentation only. No bucket, token, SDK, sentinel, provider connection, commit, push
or application deployment was created by this gate.

# Registro operacional — 21/09/2026

## P16-H3-E-D — Real-provider evidence closeout

### Objetivo e autoridade

Fechamento exclusivamente documental das evidências H3-E-A, H3-E-B, H3-E-B-R1, H3-E-B-R2 e
H3-E-C na branch `phase/p16-staging-deployment`, com baseline e tracking remoto
`84bf88b3d792b496b0eca809223f8992531778d1`. O worktree inicial estava limpo e o staging vazio.

Nenhum teste de provedor foi reexecutado. Não houve acesso à Cloudflare, solicitação ou leitura de
credenciais, mudança em código, pacote, Prisma, migration ou arquivo `.env`, nem deploy.

### Evidência reconciliada

H3-E-A passou somente o gate de pré-provisionamento. Foram congelados o nome pretendido
`lessenc-digital-staging-private`, jurisdição padrão, localização Automatic, classe Standard,
requisitos de privacidade, papéis separados das identidades runtime/operacional e o procedimento
posterior de provisionamento/validação. Não houve contato com o provedor real Cloudflare/R2 durante
H3-E-A: nenhum bucket ou objeto foi criado, nenhuma credencial real foi usada e nenhum acesso ao
provedor foi validado. O estado real do bucket e dos objetos foi estabelecido e reconciliado
somente na sequência H3-E-B posterior. Os três fatos de exposição seguintes são evidência
OWNER-CONFIRMED no dashboard da Cloudflare, não prova independente por API/provedor: o bucket
resultante é privado, seu `r2.dev` público está desabilitado e não há domínio customizado.

A primeira tentativa H3-E-B falhou fechada em `SECURE_INPUT / FAIL`. O estado remoto foi tratado
como desconhecido, sem repetição cega do provisionamento e sem inferência a partir de evidência
ausente.

H3-E-B-R1 usou somente a identidade runtime read-only; a identidade operacional não foi solicitada.
O objeto `validation/p16-h3e-readonly.txt` e o sentinel `_health/p16-readiness` estavam
`PRESENT_AND_EXACT`, estabelecendo `REMOTE_STATE / FULLY_PROVISIONED`. Não houve escrita, exclusão,
listagem ou presigning, e o repositório permaneceu byte-identical.

H3-E-B-R2 comprovou identidades runtime e operacional distintas. A leitura real com runtime passou;
a tentativa de escrita condicional runtime retornou `403 / AccessDenied`; a tentativa com a
identidade operacional atingiu `412 / PreconditionFailed`. A precondição falsa impediu mutação
efetiva. Os dois objetos permaneceram exatos; não houve `DeleteObject`, `ListObjects` ou presigning;
a validação direta do provedor passou e o repositório permaneceu byte-identical.

A identidade runtime é bucket-scoped `Object Read only`, com leitura real comprovada e escrita
efetiva negada. A identidade operacional distinta é bucket-scoped `Object Read & Write` e não está
configurada como runtime.

H3-E-C validou a configuração R2 real no parser canônico com `APP_ENV=staging`, driver `hosted`,
provider `r2`, região `auto` e healthcheck key exata. A factory de produção H3-D acessou o R2 real,
verificou o sentinel via `HeadObject`, não fez `GetObject` no sentinel e retornou
`PRIVATE_STORAGE` ready. `runReadinessProbe()` combinou banco isolado sinteticamente com R2 real e
retornou readiness global `READY`.

O caminho real H3-C utilizou `createConfiguredPrivateFileStorage()`,
`ConfiguredPrivateFileStorage` e `S3CompatiblePrivateResourceStorage`. `stat()` executou
`HeadObject` real, `open()` executou `GetObject` real com streaming, o caminho de consistência
ETag/IfMatch passou e o corpo controlado era exato. A política de chave de conteúdo rejeitou
`_health/p16-readiness`, preservando o namespace operacional fora da entrega ao buyer.

O HTTP local in-process autorizado retornou `200 / {"status":"ready"}`. O não autorizado retornou
`404 / {"status":"not_found"}` e fez zero chamadas ao provedor, comprovando autenticação antes do
acesso externo. A projeção pública permaneceu sanitizada; falha sintética do provedor apareceu
somente como `not_ready`, e o check de vazamento na saída capturada passou.

### Operações reais e limites

As operações reais exatas em H3-E-C foram:

- `HeadObject` do sentinel pela sonda H3-D direta;
- `HeadObject` do sentinel pela composição de readiness;
- `HeadObject` do objeto controlado por H3-C `stat()`;
- `GetObject` streaming do objeto controlado por H3-C `open()`;
- `HeadObject` do sentinel pelo HTTP autorizado;
- nenhuma escrita, exclusão, listagem, URL assinada ou leitura do corpo do sentinel.

`_health/p16-readiness` é um sentinel operacional intencional de zero bytes.
`validation/p16-h3e-readonly.txt` é infraestrutura controlada de validação de staging. Nenhum dos
dois é conteúdo de buyer/produto.

A aplicação continua backend-proxied e limitada a `HeadObject`/`GetObject`, sem `PutObject`,
`DeleteObject`, `ListObjects` ou presigning.

### Classificação de evidência e estado

- testes anteriores H3-C/H3-D: validação sintética;
- H3-E-B: validação direta no provedor real;
- H3-E-C: validação real do provedor pelos caminhos da aplicação;
- H3-E-C: validação HTTP local in-process;
- HTTP deployed/hosted: não validado;
- produção: não validada.

Estado reconciliado: H3-E-A COMPLETE, H3-E-B COMPLETE e H3-E-C COMPLETE. R2 real está provisionado;
acesso direto, H3-C real, H3-D real e readiness HTTP local estão validados. Nenhum deploy da
aplicação foi executado. P16 permanece incompleta, com HTTP deployed/hosted e os demais gates
operacionais externos ainda pendentes.

### Escopo documental

Arquivos documentais reconciliados nesta sessão:

- `MEMORY.md`;
- `docs/architecture/p16-staging-deployment-architecture.md`;
- `docs/operations/p16-internal-gate.md`;
- `docs/operations/p16-staging-runbook.md`;
- `memory/2026-09-21.md`.

Nenhum endpoint real, Account ID, Access Key ID, Secret Access Key, request ID do provedor, valor de
ETag ou fingerprint de credencial foi registrado. Commit, push, PR, merge, tag e deploy não foram
executados. Próximo gate: H3-E-D PRE-COMMIT REVIEW.

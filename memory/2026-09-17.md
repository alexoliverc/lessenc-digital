# Registro operacional — 2026-09-17

## P13-C — Acquisition Journey & Order Attribution

Status:

`IMPLEMENTATION COMPLETE / PASS / DOCUMENTED / NOT YET INTEGRATED / NOT YET CHECKPOINTED`

Branch:

`phase/p13-c-acquisition-order-attribution`

Starting baseline and unchanged implementation HEAD:

`8f501575078a589a6712a3685f92548e6e89def3`

## Implementation completed

- first-party AcquisitionJourney;
- canonical five-key UTM allowlist;
- sanitized landing path;
- sanitized external referrer hostname;
- immutable First Touch;
- eligible Last Touch;
- direct/internal attribution preservation;
- concurrency-safe Journey persistence;
- Next.js Proxy Journey-cookie boundary;
- acquisition server adapter;
- prefetch exclusion;
- immutable OrderAttribution;
- explicit unattributed snapshots;
- atomic Order + OrderAttribution transaction;
- rollback protection;
- sequential `EXISTING` immutability;
- concurrent CREATED + EXISTING behavior.

## Runtime proof

Live production-server HTTP + MySQL validation proved:

- campaign acquisition;
- cookie issuance/reuse;
- canonical UTM persistence;
- unknown/sensitive query exclusion;
- sanitized referrer;
- direct/internal preservation;
- external referral Last Touch advancement;
- immutable First Touch;
- zero acquisition capture for Purpose prefetch;
- zero acquisition capture for Next-Router-Prefetch;
- zero acquisition capture for Sec-Purpose prefetch.

## Atomic Order attribution proof

Real isolated MySQL validation proved:

- Order and attributed snapshot commit together;
- unattributed Orders receive one explicit nullable snapshot;
- forced snapshot failure rolls back Customer + Order + OrderItem;
- later Journey cannot rewrite an `EXISTING` Order;
- concurrent duplicate creation resolves as one CREATED + one EXISTING;
- exactly one winner-owned OrderAttribution remains.

## Final validation

- unit/application: 48 files / 498 tests PASS;
- MySQL integration: 19 files / 165 tests PASS;
- focused P13-C unit/boundary: 63 tests PASS;
- focused P13-C MySQL: 10 tests PASS;
- Prisma validate PASS;
- typecheck PASS;
- ESLint PASS;
- production build PASS;
- `git diff --check` PASS;
- schema/migrations unchanged.

## Documentation

Created:

- `docs/operations/p13-c-final-gate.md`;
- `memory/2026-09-17.md`.

Updated:

- `docs/architecture/p13-phase-execution-brief.md`;
- `docs/architecture/p13-analytics-attribution-growth-contract.md` implementation-status surface;
- `ROADMAP.md`;
- `docs/README.md`;
- `MEMORY.md`.

Architecture Freeze R2 was not reopened.

Historical P13-B records remain preserved.

## Git / deployment

Not performed:

- git add;
- commit;
- push;
- PR;
- merge;
- checkpoint/tag;
- deploy.

Next:

complete worktree/diff audit before owner-authorized Git lifecycle.

# Registro operacional — 2026-09-11

## Sessão

Foi definida a criação do repositório inicial da L'Essenc Digital para conectar Codex, VS Code e Git no computador Windows. O scaffold foi criado com governança, memória, configuração do editor, documentação de conectividade e script PowerShell de bootstrap.

## Objetivo da sessão

Preparar governança, memória, configuração do editor e bootstrap PowerShell para o diretório `C:\Projetos\lessenc-digital`, além de inicializar o Git do scaffold.

## Próximo movimento

Executar o bootstrap no Windows, revisar a estrutura local, criar o repositório remoto privado e iniciar a especificação técnica do MVP.

## Atualização — ambiente de desenvolvimento

Foi concluída a configuração do ambiente local da L'Essenc Digital.

### Validado

- diretório definitivo `C:\Projetos\lessenc-digital`;
- Git inicializado na branch `main`;
- VS Code com perfil `L'Essenc`;
- Node.js 24.19.0;
- npm 11.17.0;
- Corepack 0.35.0;
- pnpm 11.26.0;
- ESLint, Prettier e EditorConfig;
- Codex CLI funcional;
- launcher `codex-lessenc`;
- perfil Codex específico com `workspace-write` e `on-request`;
- ripgrep 15.2.0;
- terminal configurado e validado em UTF-8;
- `.env` e `.env.local` ignorados pelo Git;
- `.env.example` preparado para versionamento.

## Próximo movimento

Finalizar a governança do agente, revisar o diff completo e criar o primeiro commit do repositório antes da configuração do remoto privado.

## Atualização — governança do agente

A governança operacional do Codex foi revisada e concluída.

### Regras consolidadas

- autonomia limitada ao escopo solicitado;
- aprovação obrigatória para dependências, arquitetura, banco, autenticação, pagamentos, infraestrutura e ações externas;
- proteção contra comandos Git destrutivos;
- push, merge, rebase e tags dependem de autorização;
- precedência formal de fontes de verdade;
- política explícita para segredos e arquivos `.env`;
- protocolo obrigatório de início e encerramento de sessão;
- memória consolidada separada do histórico operacional.

### Próximo movimento

Criar o primeiro commit oficial do repositório e, depois, configurar o repositório remoto privado.

## Atualização — baseline remoto

O baseline técnico inicial foi versionado e publicado no repositório privado do GitHub.

### Git

- primeiro commit: `0a12083`;
- branch principal: `main`;
- remoto: `origin`;
- `main` configurada para rastrear `origin/main`;
- repositório remoto privado criado;
- bootstrap e governança concluídos.

### Nova fase

O projeto entra em `MVP-ARCH-01 — Arquitetura técnica do MVP`.

O desenvolvimento de novas fases não deverá ocorrer diretamente na `main`; será utilizado fluxo por branches de trabalho.

## Próximo movimento

Produzir e aprovar a arquitetura técnica geral da operação digital antes da implementação.

## Atualização — LES-ARCH-DIG-R01 aprovado

A arquitetura técnica geral da L'Essenc Digital foi revisada e aprovada.

### Decisões consolidadas

- backend será a autoridade para pagamentos, autorização e entrega;
- página de agradecimento não comprova pagamento;
- ativos digitais permanecerão privados;
- acesso será concedido por entitlement server-side;
- Mercado Pago será encapsulado como integração externa;
- Hostinger será infraestrutura inicial sem acoplamento estrutural;
- segurança e observabilidade serão requisitos transversais;
- painel administrativo utilizará as mesmas regras de domínio e autorização;
- a plataforma será pequena em superfície, mas não descartável em arquitetura.

### Próximo documento

`LES-DATA-DIG-R01` — Modelo de Dados e Estados.

## Atualização — LES-DATA-DIG-R01 aprovado

O modelo lógico de dados e estados da L'Essenc Digital foi revisado e aprovado.

### Decisões consolidadas

- `Order`, `Payment`, `Entitlement` e `Delivery` são entidades distintas;
- um pedido pode possuir múltiplas tentativas de pagamento;
- pagamentos possuem estados internos independentes do provedor;
- eventos financeiros terão registro próprio e processamento idempotente;
- o acesso ao produto será representado por `Entitlement`;
- ativos digitais serão privados;
- entregas possuirão ciclo de vida próprio;
- dinheiro será armazenado com representação exata;
- timestamps serão persistidos de forma consistente;
- registros financeiros e de auditoria preservarão histórico;
- transições críticas deverão possuir consistência transacional;
- a plataforma deverá permitir reconciliação com o provedor de pagamentos.

### Próximo documento

`LES-FLOW-DIG-R01` — Fluxos Funcionais e Máquinas de Estado.

## Atualização — LES-FLOW-DIG-R01 aprovado

Os fluxos funcionais e máquinas de estado da L'Essenc Digital foram revisados e aprovados.

### Decisões consolidadas

- estados financeiros serão controlados pelo backend;
- página de agradecimento não determina pagamento;
- webhooks serão tratados com idempotência;
- múltiplas tentativas de pagamento serão suportadas;
- regressões inválidas de estado serão bloqueadas;
- entitlement será criado somente após confirmação financeira válida;
- entrega digital terá ciclo de vida independente do entitlement;
- reembolso e chargeback poderão revogar acesso;
- reconciliação utilizará as mesmas regras de transição;
- falhas intermediárias deverão ser recuperáveis;
- ações administrativas críticas serão auditadas;
- analytics não será fonte de verdade financeira.

### Próximo documento

`LES-INT-MP-R01` — Integração Mercado Pago.

## Atualização — LES-INT-MP-R01 aprovado

A arquitetura de integração com Mercado Pago foi revisada e aprovada.

### Decisões consolidadas

- Mercado Pago será encapsulado por adapter;
- Access Token permanecerá exclusivamente no backend;
- dados brutos de cartão não serão persistidos pela L'Essenc;
- valores financeiros serão determinados pelo backend;
- criação de pagamentos utilizará idempotência;
- IDs externos permanecerão separados dos IDs internos;
- webhooks serão autenticados antes de produzir efeitos;
- eventos duplicados serão tratados de forma idempotente;
- estado financeiro será confirmado server-side;
- estados externos serão normalizados para estados internos;
- estados desconhecidos nunca liberarão acesso;
- timeouts serão tratados como resultado potencialmente desconhecido;
- retries só ocorrerão quando forem seguros;
- reconciliação fará parte da confiabilidade financeira;
- reembolso e chargeback utilizarão a mesma máquina de estados;
- ambientes de teste e produção permanecerão isolados;
- logs não poderão expor segredos financeiros.

### Próximo documento

`LES-SEC-DIG-R01` — Arquitetura de Segurança da Operação Digital.

## Atualização — LES-SEC-DIG-R01 aprovado

A arquitetura geral de segurança da L'Essenc Digital foi revisada e aprovada.

### Decisões consolidadas

- OWASP ASVS Level 2 será o baseline de verificação;
- autenticação administrativa terá MFA;
- autorização será obrigatoriamente server-side;
- sessões utilizarão cookies seguros;
- entradas serão validadas por schema;
- controles contra IDOR/BOLA, injection, XSS, CSRF e SSRF farão parte da arquitetura;
- ativos digitais permanecerão privados;
- uploads serão tratados como entrada não confiável;
- segredos terão ciclo de vida, rotação e revogação;
- logs não poderão conter segredos ou dados financeiros sensíveis;
- ambientes serão segregados;
- supply chain fará parte do modelo de ameaça;
- backup e restore serão requisitos operacionais;
- resposta a incidentes e kill switches serão previstos;
- testes de segurança farão parte do Definition of Done.

### Próximo documento

`LES-ADMIN-DIG-R01` — Arquitetura do Painel Administrativo.

## Atualização — LES-ADMIN-DIG-R01 aprovado

A arquitetura do painel administrativo da L'Essenc Digital foi revisada e aprovada.

### Decisões consolidadas

- o painel será uma camada operacional sobre regras de domínio;
- nenhuma operação crítica fará alteração direta de banco;
- dashboard, produtos, pedidos, pagamentos, clientes, acessos, entregas, auditoria e configurações compõem o escopo inicial;
- estados financeiros não poderão ser alterados arbitrariamente pela interface;
- alterações de preço não modificarão histórico de pedidos;
- ativos digitais serão versionados e mantidos em storage privado;
- OWNER, ADMIN e SUPPORT terão responsabilidades distintas;
- permissões serão sempre verificadas server-side;
- ações críticas exigirão confirmação e auditoria;
- reembolso, revogação e reativação utilizarão comandos explícitos;
- paginação e filtros serão executados server-side;
- dados sensíveis e segredos não serão expostos no painel;
- analytics publicitário ficará desativado no ambiente administrativo;
- operações administrativas críticas produzirão AuditEvent e correlationId.

### Próximo documento

`LES-OBS-DIG-R01` — Observabilidade, Logs, Métricas e Alertas.

## Atualização — LES-OBS-DIG-R01 aprovado

A arquitetura de observabilidade da L'Essenc Digital foi revisada e aprovada.

### Decisões consolidadas

- logs, métricas, traces e AuditEvents terão responsabilidades distintas;
- operações relevantes utilizarão correlationId;
- logs de produção serão estruturados;
- segredos e dados financeiros sensíveis serão proibidos em logs;
- PII será minimizada ou mascarada;
- métricas financeiras, de webhook, entitlement, delivery e segurança serão monitoradas;
- health checks de liveness e readiness serão previstos;
- invariantes financeiras serão monitoráveis;
- divergências de reconciliação gerarão sinais operacionais;
- alertas serão acionáveis e possuirão controle contra alert fatigue;
- observabilidade será separada de analytics de marketing;
- falhas críticas não poderão permanecer silenciosas;
- instrumentação será progressiva e provider-neutral;
- OpenTelemetry poderá ser adotado conforme necessidade;
- observabilidade fará parte do Definition of Done.

### Próximo documento

`LES-DEPLOY-DIG-R01` — Deploy, Ambientes e Operação.

## Atualização — LES-DEPLOY-DIG-R01 aprovado

A arquitetura de deploy, ambientes e operação da L'Essenc Digital foi revisada e aprovada.

### Decisões consolidadas

- Hostinger Cloud Startup será a infraestrutura inicial;
- produção utilizará aplicação Node.js/Next.js gerenciada;
- LOCAL, TEST e PRODUCTION serão ambientes separados;
- produção utilizará apenas código aprovado;
- pnpm e lockfile serão parte do build reproduzível;
- segredos permanecerão fora do Git;
- configurações serão fornecidas por ambiente;
- banco de produção será isolado;
- migrations serão versionadas e testadas antes de produção;
- migrations destrutivas terão tratamento especial;
- rollback de aplicação será separado de rollback de banco;
- backups e testes de restore farão parte da operação;
- storage privado será desacoplado da infraestrutura de aplicação;
- domínio, DNS e HTTPS terão procedimento controlado;
- health checks e smoke tests serão obrigatórios;
- go-live financeiro terá checklist próprio;
- deploy será acompanhado por observabilidade;
- disaster recovery e vendor exit fazem parte da arquitetura;
- rollout inicial ocorrerá progressivamente.

### Marco concluído

A sequência principal da arquitetura técnica do MVP foi concluída:

- LES-ARCH-DIG-R01
- LES-DATA-DIG-R01
- LES-FLOW-DIG-R01
- LES-INT-MP-R01
- LES-SEC-DIG-R01
- LES-ADMIN-DIG-R01
- LES-OBS-DIG-R01
- LES-DEPLOY-DIG-R01

### Próxima etapa

Consolidação arquitetural e checklist de readiness para implementação.

## Atualização — Architecture Readiness Gate aprovado

O Architecture Readiness Review da L'Essenc Digital foi concluído com resultado PASS.

### Resultado

- os oito documentos principais da arquitetura estão aprovados;
- não foram identificados bloqueadores arquiteturais para implementação;
- segurança, pagamentos, dados, fluxos, administração, observabilidade e deploy possuem baseline definida;
- pendências operacionais restantes não bloqueiam o início do desenvolvimento;
- o LES-READINESS-DIG-R01 formaliza a conclusão da fase MVP-ARCH-01.

### Fase concluída

`MVP-ARCH-01 — Arquitetura técnica do MVP`

### Próxima fase autorizada

`MVP-IMPL-01 — Foundation & Project Scaffold`

A MVP-IMPL-01 deverá começar somente após a baseline arquitetural ser integrada à branch `main`.

### Próxima ação

Publicar a branch `phase/mvp-architecture-r01`, revisar a integração e incorporar a baseline arquitetural à `main`.

## Atualização — MVP-IMPL-01 iniciada

A baseline arquitetural foi integrada à `main` e sincronizada com `origin/main`.

A branch `phase/mvp-impl-01-foundation` foi criada a partir do commit `36508de`.

### Objetivo da fase

Construir a fundação técnica executável do MVP antes da implementação dos módulos de negócio.

### Escopo inicial

- Next.js e React;
- TypeScript estrito;
- ESLint e Prettier;
- Vitest;
- configuração de ambiente validada;
- estrutura modular;
- logging estruturado inicial;
- health endpoint;
- quality gates locais.

Banco, ORM e migrations permanecerão para um lote separado que exigirá aprovação específica.

## Atualização — Foundation Batch 01 concluído

A fundação técnica executável da L'Essenc Digital foi implementada e validada na branch `phase/mvp-impl-01-foundation`.

### Stack consolidada

- Next.js 16.3.4;
- React 19.3.0;
- React DOM 19.3.0;
- TypeScript 6.0.3 em modo estrito;
- ESLint 10.10.0 com plugin oficial do Next;
- typescript-eslint 8.70.0;
- React Hooks ESLint 7.1.1;
- Prettier 3.9.6;
- Vitest 5.0.0;
- Zod 4.6.2.

### Fundação implementada

- App Router do Next.js;
- página raiz mínima;
- configuração de ambiente server-side com Zod;
- logger estruturado inicial com redaction de chaves sensíveis;
- módulo de health;
- endpoint `GET /api/health`;
- teste automatizado do health module;
- TypeScript strict;
- lint;
- formatação;
- build de produção.

### Quality gates

Todos os gates foram aprovados:

- Prettier: PASS;
- ESLint: PASS;
- TypeScript: PASS;
- Vitest: PASS — 1 teste aprovado;
- Next.js production build: PASS;
- `git diff --check`: PASS.

### Smoke test

O servidor de produção foi iniciado localmente em `127.0.0.1:31270`.

O endpoint `/api/health` respondeu:

- HTTP 200;
- body `{"status":"ok"}`;
- `Cache-Control: no-store`.

O servidor foi encerrado após a validação.

### Restrições mantidas

Nenhum banco, ORM, migration, integração Mercado Pago, autenticação ou infraestrutura externa foi implementado neste lote.

Esses itens permanecem sujeitos aos gates de aprovação definidos em `AGENTS.md`.

## Atualização — roadmap e modelo operacional formalizados para revisão

### Objetivo e autorização da sessão

O owner pausou a implementação para formalizar o roadmap até produção e o modelo ChatGPT → Codex → ChatGPT review. Autorizou somente alterações documentais em quatro arquivos e determinou parada para revisão, sem commit ou progressão de fase.

### Estado verificado na abertura

- Diretório confirmado: `C:\Projetos\lessenc-digital`.
- Branch: `phase/mvp-impl-01-foundation`, inicialmente sem alterações pendentes.
- Commit local atual: `4507a27` — `feat: establish MVP foundation scaffold`.
- Leitura integral de AGENTS.md, MEMORY.md, deste registro diário e dos documentos atuais de arquitetura/readiness, além dos índices, README e políticas operacionais relevantes.

### Decisões registradas

- `GOV-01 — Project Governance & Repository Bootstrap`, `MVP-ARCH-01 — Architecture Baseline` e `MVP-IMPL-01 — Foundation & Project Scaffold` estão concluídas conforme decisão do owner e evidências históricas.
- O roadmap formal `docs/LES-ROADMAP-DIG-R01.md` está em processo de adoção, aguardando revisão técnica do ChatGPT.
- O modelo ChatGPT → Codex → ChatGPT review está adotado: ChatGPT dirige, planeja e revisa; Codex executa somente o escopo autorizado, na branch da fase, e produz evidências.
- Cada nova fase exige Phase Execution Brief; instruções abertas como "continue o projeto" não autorizam implementação.
- PASS WITH FIXES exige correções e revisão antes da progressão; FAIL exige correção ou redefinição do brief. PASS técnico não autoriza operações protegidas pelo owner.
- Próximo candidato: `MVP-IMPL-02 — Data & Persistence Foundation`; sua execução ainda não está autorizada, incluindo banco, ORM, schema e migrations.
- O alvo MySQL permanece o da arquitetura aprovada. Staging corresponde ao ambiente TEST existente. Segurança e observabilidade continuam transversais.

### Arquivos e mudanças

- Criado `docs/LES-ROADMAP-DIG-R01.md`, com 18 fases, cada uma contendo objetivo, escopo, entregas, dependências, approval gates, critérios de saída e status; inclui gates comuns e pendências documentais.
- Acrescentada a seção 15 a `AGENTS.md`, preservando as regras anteriores, com papéis, autorizações, fluxo obrigatório, critérios de revisão e conteúdo mínimo do Phase Execution Brief.
- Atualizado `MEMORY.md` com o scaffold concluído, commit atual, adoção do modelo operacional, roadmap em revisão e candidato seguinte não autorizado.
- Acrescentado este registro ao histórico diário, preservando as sessões anteriores.

### Conflitos e pendências identificados

- O readiness antigo incluía banco, ORM e CI no escopo de MVP-IMPL-01. A decisão mais recente do owner encerra o scaffold e separa Data & Persistence na MVP-IMPL-02; CI não foi declarado entregue e está planejado em OPS/DEPLOY.
- `docs/architecture/LES-FLOW-DIG-R01.md` termina incompleto na seção 25, em `compar`, com bloco Markdown aberto. Confirmar/complementar com o ChatGPT antes de implementar regras dependentes; arquivo não alterado nesta sessão.
- O README atribui papéis amplos ao Codex e alguns índices ainda descrevem documentos existentes como futuros. A nova decisão e AGENTS.md regem o trabalho; harmonização desses textos fica pendente de escopo próprio.
- Documentos de oferta/copy/produto/políticas, ORM/modelo físico, storage/acesso posterior, domínio e detalhes operacionais permanecem como dependências das fases correspondentes.
- A memória pré-commit exigida pelas regras anteriores foi compatibilizada com o Memory Update ao final do novo fluxo: registrar a sessão sempre e consolidar somente operações efetivamente realizadas.

### Validação e limites

- Validação documental concluída: `git diff --check` passou sem erros. A revisão estrutural confirmou as 18 fases na ordem solicitada, os sete campos obrigatórios em cada fase, links locais resolvidos, preservação das regras preexistentes de AGENTS.md e do histórico diário, e ausência de marcadores de conflito ou padrões de credencial nos quatro arquivos da alteração.
- `git status --short --branch` final: branch `phase/mvp-impl-01-foundation`; modificados `AGENTS.md`, `MEMORY.md` e `memory/2026-09-11.md`; novo arquivo não rastreado `docs/LES-ROADMAP-DIG-R01.md`.
- Git emitiu apenas o aviso de normalização de fim de linha em `AGENTS.md` (`CRLF` para `LF` no próximo toque do Git); não é falha de conteúdo, não altera a regra de parada e não foi feita conversão deliberada de arquivos.
- Testes, lint, typecheck e build da aplicação não reexecutados: alteração exclusivamente documental, sem mudança de comportamento executável. Os resultados do Foundation Batch 01 acima são evidências históricas.
- Nenhuma dependência instalada; nenhum código de aplicação, banco, ORM, schema, migration, autenticação, pagamento, webhook ou infraestrutura alterado. Arquivos `.env` não foram lidos ou alterados.
- Nenhum commit, push, merge, rebase, tag ou operação destrutiva executado.

### Próximo movimento

Entregar o diff e o relatório documental para revisão do ChatGPT. Parar e aguardar revisão do owner; não iniciar MVP-IMPL-02 e não fazer commit nesta rodada.

## Atualização — missão posterior de consolidação LES-DIG P00–P04

O owner forneceu nova missão documental que substitui a posição de roadmap, o primeiro produto e a baseline técnica da rodada acima. As ações anteriores foram preservadas no Git e neste histórico; a instrução antiga de não fazer commit referia-se à rodada anterior. A missão nova autoriza commit documental condicional à validação e autoriza tag após confirmação do commit, mas não autoriza push, merge, infraestrutura nem migração física.

### Verificação prévia e objetivo

- `git rev-parse --show-toplevel`: `C:/Projetos/lessenc-digital`.
- Branch: `phase/mvp-impl-01-foundation`; HEAD na abertura: `4507a27`. O `git status` inicial listava as quatro alterações documentais da rodada anterior, que foram preservadas e reconciliadas.
- Após conferir a base e preparar somente caminhos do escopo, criada a branch local `phase/docs-p00-p04-baseline` a partir de `4507a27`; as alterações anteriores foram mantidas sem reset nem descarte.
- Lidos AGENTS.md, MEMORY.md, memory/README.md, registro diário mais recente e documentação do módulo em docs/, após constatar que ROADMAP.md não existia.
- Objetivo: materializar a baseline documental P00–P04 como memória técnica oficial, sem alterar o comportamento executável.

### Decisões atuais e reconciliação

- P00–P03 documentação COMPLETE; P04 specification COMPLETE e physical implementation PENDING. Próxima fase documental P05 — Design System.
- Primeiro produto atual: Cronograma Capilar Inteligente por R$ 29,90 (`2990 BRL`), meta comercial de dez vendas concluídas por dia até o segundo mês; o antigo produto/preço permanece apenas histórico.
- Arquitetura documental posterior: monólito modular, Prisma/MySQL, transactional outbox, `APP_ENV` e quatro ambientes LOCAL/TEST/STAGING/PRODUCTION; npm com package-lock e pipeline npm ci. O código físico anterior em `4507a27` usa pnpm, versões diferentes e não tem Prisma: **nenhuma migração física foi feita**.
- A sequência `GOV/MVP-*` e `docs/LES-ROADMAP-DIG-R01.md` foi preservada com aviso histórico; o ROADMAP.md atual define P00–P26. Documentos LES-*-R01 receberam avisos de precedência, não tiveram suas decisões antigas reescritas.
- O arquivo histórico LES-FLOW-DIG-R01.md permanece truncado na seção 25. A nova baseline P03 contém estados e invariantes definidos pela missão; demais casos não expressos permanecem OPEN.
- ADR-0001 a ADR-0010 estão aceitos como especificação documental por decisão expressa da missão, não como prova de implantação.

### Alterações e limites

- Criados ROADMAP.md, 5 documentos de produto P01, 21 documentos de arquitetura P02–P04, 2 documentos de segurança, 10 ADRs, 3 índices de diretório e documentação operacional de índice. `docs/README.md`, `docs/product/README.md`, `docs/architecture/README.md`, README.md, MEMORY.md e AGENTS.md foram atualizados; histórico anterior no diário e nos documentos LES-*-R01 foi mantido.
- Nenhuma página comercial, checkout, integração, banco, schema, migration, autenticação, admin, ebook delivery, infraestrutura, dependência ou refactor de aplicação foi implementado nesta sessão. Nenhuma credencial real foi lida ou registrada.
- Provedores de autenticação, email, storage privado, observabilidade, rate limit distribuído e framework E2E permanecem OPEN; detalhes de política de reembolso, conteúdo final e domínio também aguardam decisão.

### Validação e próximo movimento

Revisão estrutural documental passou: 28 documentos específicos P01–P04 presentes, 10 ADRs `ACCEPTED`, 175 links locais válidos, sem referência a Smith Sterling ou espaços ao final das linhas. `pnpm exec prettier --check` dos documentos e índices novos passou. Check físico do scaffold legado: `pnpm run lint` PASS; `pnpm run typecheck` PASS; `pnpm run test` PASS (1 teste); `pnpm run build` PASS, rotas `/` e `/api/health`. Essas execuções não validam a implementação P04 pretendida com npm/Prisma.

Revisar diff total (inclusive arquivos não rastreados), integridade documental, segredos, `git status`, `git diff --check`, `git diff --stat` e versionamento apenas dos caminhos do escopo. Não iniciar P04 físico; P05 é apenas a próxima fase documental. Commit local e tag têm autorização expressa e condicional na missão atual, sujeitos à revisão final; nenhum push/merge foi autorizado.

### Fechamento da execução documental

- Os 60 arquivos documentais foram preparados no índice da branch `phase/docs-p00-p04-baseline`: 43 criados e 17 modificados frente a `4507a27`. A revisão incluiu os arquivos novos e as quatro alterações locais recebidas da sessão anterior; o diff staged final contém `1031 insertions(+), 68 deletions(-)`.
- `git diff --check` e `git diff --cached --check`: PASS, sem saída. `git diff --stat` vazio porque todo o escopo está staged; `git diff --cached --stat` mostra a consolidação completa. Nenhum arquivo fora de Markdown entrou no índice; busca nas linhas adicionadas por formatos comuns de credenciais não encontrou ocorrências.
- `pnpm run lint`, `pnpm run typecheck`, `pnpm run test` e `pnpm run build` passaram sobre o scaffold físico legado. Essas verificações não substituem testes da especificação P04 pendente.
- O commit e a tag expressamente autorizados pela missão permanecem pendentes da revisão técnica independente do ChatGPT e do Final Quality Gate previstos no fluxo obrigatório de `AGENTS.md` §15. A validação do Codex não equivale a essa revisão. Nenhum commit, tag, push ou merge executado nesta missão até este registro; P04 físico e P05 não iniciados.

## Atualização — correções do Final Quality Gate P00–P04

O ChatGPT classificou a consolidação como **PASS WITH FIXES** e delimitou correções documentais, sem commit ou tag nesta etapa. A branch permanece `phase/docs-p00-p04-baseline`, baseada em `4507a27`; o trabalho staged anterior foi preservado.

- `AGENTS.md` e `docs/README.md` agora enumeram a precedência das sete fontes. Documento histórico ou substituído não é normativo: **CURRENT CANONICAL BASELINE WINS**.
- `MEMORY.md`, a toolchain P04 e a documentação operacional distinguem o scaffold físico `pnpm`/`pnpm-lock.yaml` da meta P04 `npm` 11.19.1/`package-lock.json`/`npm ci`. A diferença é intencional e continua aberta até implementação física P04 autorizada. Foram explicitadas as proibições de migração, instalação, segundo lockfile, remoção do lockfile atual e upgrade automático.
- Os documentos anteriores de arquitetura, roadmap e conectividade receberam status visível `HISTORICAL / SUPERSEDED`; `LES-FLOW-DIG-R01.md` foi marcado `HISTORICAL / INCOMPLETE`, preservando integralmente o corpo truncado. Produto, preço, oferta e ambientes antigos não orientam implementação atual.
- Nenhum código de aplicação, dependência, lockfile, banco ou infraestrutura foi alterado. Commit, tag, push e merge permanecem não executados; retornar as evidências e o diff ao ChatGPT para revisão das correções.

# Registro operacional — 2026-09-12

## P04 — reconciliação física da fundação

Objetivo autorizado pelo brief do owner: migrar o scaffold físico de pnpm para npm, preservar versões diretas revalidadas, implantar a fundação `APP_ENV` e validar instalação limpa, qualidade e health. Fora de escopo: Prisma/MySQL, banco, pagamentos, autenticação, infraestrutura, deploy e P05. Esta rodada não autoriza commit, tag, push ou merge.

### Abertura e segurança

- Diretório `C:\Projetos\lessenc-digital`, branch `phase/p04-physical-reconciliation`, HEAD `71488f16f6f127de952efca47297078ef32b4f0e`, árvore limpa, tag `checkpoint/p04-baseline-reconciled` no HEAD.
- Remoto consultado sem alteração: `origin/main` em `3512142b747221c9151b4f8b882f99d647b19121`; branch P04 ausente no remoto.
- Host: Node 24.19.0, npm 11.17.0 e pnpm 11.26.0. Nenhum gerenciador local de versões estabelecido foi encontrado; a atualização global do host não foi improvisada.
- O checkpoint anterior usa `pnpm-lock.yaml`, versões diretas revalidadas Next 16.3.4/React 19.3.0/Vitest 5.0.0, e não tem Prisma. `node_modules` é gerado, ignorado e não versionado.

### Execução e validação

- `package.json`: `check` passou a usar `npm run`; `packageManager` reflete npm 11.17.0 efetivamente disponível, sem declarar 11.19.1 não instalado no host.
- `src/lib/config/env-schema.ts`, `env.ts` e teste: `APP_ENV` exige `local`, `test`, `staging` ou `production`; `NODE_ENV` e o default de `APP_URL` foram preservados. `.env.example` recebeu apenas `APP_ENV=local` fictício.
- A primeira geração de lockfile com npm falhou no Arborist ao inspecionar links em `node_modules` criado pelo pnpm (`Cannot read properties of null (reading 'matches')`). A pasta gerada e não versionada foi movida, após verificação de caminho, para `C:\Users\Alex Oliveira\AppData\Local\Temp\lessenc-p04-node_modules-71488f1` como recuperação temporária.
- Uma tentativa com `--prefer-offline` encontrou cache incompleto para `@types/react@19.3.0`; `npm view` confirmou que a versão existe. A geração normal concluiu com `package-lock.json` v3 (268 entradas), mantendo todos os pins diretos. `pnpm-lock.yaml` foi removido; resta um único lockfile de projeto.
- `npm ci` foi executado sem `node_modules` no checkout e passou: 209 pacotes instalados, auditoria npm com 0 vulnerabilidades. `npm ls --depth=0` confirmou as versões diretas; não há `node_modules/.pnpm` nem Prisma. O lockfile registra somente `fsevents` como pacote transitivo opcional com script de instalação.
- `npm run format:check` PASS; `npm run lint` PASS; `npm run typecheck` PASS; `npm run test` PASS (2 arquivos, 7 testes); `$env:APP_ENV='local'; npm run build` PASS.
- Smoke de produção: bind `127.0.0.1`, porta `31271`, `GET /api/health` → HTTP 200, `{"status":"ok"}`, `Cache-Control: no-store`; processo encerrado e porta liberada.
- Auditoria de escopo/segurança: só `.env.example` versionado entre arquivos `.env`; adição `APP_ENV=local` fictícia; nenhuma dependência de Prisma/MySQL, Mercado Pago ou autenticação, nenhum hook de projeto novo ou `NEXT_PUBLIC_*`; varredura heurística de segredos sem candidatos. `git diff --check` passou durante a execução, sujeito à revisão final após documentação.
- Documentação canônica sincronizada para corrigir o target antigo em `MEMORY.md`, preservar o histórico pnpm e registrar P04 localmente validada, com revisão pendente, desvio Node/npm do host e Prisma/MySQL na P06.
- A remoção recursiva da cópia temporária antiga em `C:\Users\Alex Oliveira\AppData\Local\Temp\lessenc-p04-node_modules-71488f1` foi rejeitada automaticamente com `blocked by policy`, sem motivo adicional informado. A cópia permanece fora do repositório; a instalação npm validada está no checkout.

### Pendências

O diff de código, documentação e arquivos novos foi revisado. Após os ajustes documentais, `npm run check`, `npm run format:check` e `git diff --check` passaram; o Git permanece na branch P04 com alterações não comitadas. Entregar o Validation Report ao ChatGPT para revisão, sem commit, tag, push, merge ou progressão de fase. Atualização do host Node/npm e remoção da cópia temporária seguem pendentes.
## P04 — final validation and ChatGPT technical approval

- Host reconciliado para Node.js 24.21.0 LTS e npm 11.19.1.
- `packageManager` final: `npm@11.19.1`.
- npm é o package manager autoritativo.
- `package-lock.json` é o único lockfile final.
- `npm ci` PASS e 0 vulnerabilidades reportadas.
- lint, typecheck, 7/7 testes, format check e build PASS.
- Smoke test de produção em `127.0.0.1:31271`: HTTP 200, `{"status":"ok"}`, `Cache-Control: no-store`; processo encerrado e porta liberada.
- `git diff --check` PASS.
- Prisma, MySQL, schema e migrations permaneceram fora da P04.
- ChatGPT Technical Review: **P04 PASS**.
- P05 permanece NOT STARTED / NOT AUTHORIZED.

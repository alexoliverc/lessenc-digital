# Registro operacional — 2026-09-19

## Objetivo

Retomar a P13 a partir da branch `phase/p13-d-internal-measurement-consent`, preservar a implementação local D1–D3 existente e executar sequencialmente P13-D até P13-H, com gates técnicos internos e ciclo Git autorizado pelo owner.

## Baseline de entrada

- branch: `phase/p13-d-internal-measurement-consent`;
- HEAD/base: `ff07153ad4494c54e0984e38b7a4a26bae8b3bad`;
- `main` e `origin/main`: `ff07153ad4494c54e0984e38b7a4a26bae8b3bad`;
- worktree inicial: 10 arquivos tracked modificados e 6 arquivos novos, todos revisados como superfícies P13-D D1–D3;
- P13-A: COMPLETE / ARCHITECTURE FROZEN R2;
- P13-B: COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED;
- P13-C: COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED.

## Evidência inicial preservada

- testes direcionados D1–D3: 9 arquivos / 71 testes PASS;
- persistência Analytics MySQL direcionada: 1 arquivo / 10 testes PASS;
- typecheck: PASS;
- lint: PASS;
- Prettier nos 16 paths D1–D3: PASS;
- `git diff --check`: PASS;
- nenhum descarte, reset, clean ou rebase foi executado.

## Estado

P13-D está COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED.

## Implementação P13-D

- `AnalyticsEventRepository.createIdempotent` diferencia criação, replay compatível pelo mesmo ID e conflito em outra chave única;
- `VIEW_CONTENT` nasce somente de exposição comercial Product/Offer válida, com event ID privado gerado no Proxy e exclusão de prefetch;
- `INITIATE_CHECKOUT` nasce da resolução server-side válida do checkout, não de clique em CTA;
- token de submissão e evento compartilham o mesmo instante canônico, mas usam UUIDs distintos;
- os produtores preservam Journey, estado de atribuição, consent snapshot, schema version e valores comerciais autoritativos;
- consentimento de analytics e advertising é explícito, persistido em Journey ativa, reversível e protegido por Origin/corpo estrito;
- `UNKNOWN` não equivale a grant e advertising não pode ser granted com analytics denied;
- projeção browser-safe não contém Journey ID, PII, token ou segredo;
- falhas de measurement permanecem isoladas da oferta, checkout, Order, Payment e entitlement;
- schema Prisma, migrations, P10/P11 Outbox e `src/infrastructure/database/client.ts` não foram alterados.

## Evidências P13-D

- suíte unit/application completa: 53 arquivos / 525 testes PASS;
- suíte focada P13-D: 6 arquivos / 33 testes PASS;
- regressão MySQL isolada completa: 19 arquivos / 170 testes PASS;
- analytics persistence MySQL focada: 11/11 PASS;
- Prisma validate: PASS;
- `lessenc_test` migration status: CURRENT, 8 migrations;
- typecheck: PASS;
- lint: PASS;
- scoped Prettier: PASS;
- `git diff --check`: PASS;
- production build: PASS com segredo P12 fictício e efêmero de validação;
- `npm audit --audit-level=high`: 0 vulnerabilidades.

## Prova runtime P13-D

Servidor otimizado local + MySQL isolado confirmaram:

- página pública HTTP 200;
- checkout HTTP 200;
- consentimento inicial UNKNOWN/UNKNOWN e atualização explícita GRANTED/GRANTED;
- 1 VIEW_CONTENT com snapshot UNKNOWN/UNKNOWN;
- 1 INITIATE_CHECKOUT com snapshot GRANTED/GRANTED;
- token `issuedAt` em UTC canônico;
- igualdade exata `issuedAt == AnalyticsEvent.occurredAt` por comparação textual SQL, sem janela de tolerância;
- fixtures removidas e contagem residual Product/Offer igual a zero.

## Lifecycle Git P13-D

- implementation commit: `92f99559d32ee9658e94957f9dfc3fff8aad1ebd`;
- implementation push: COMPLETE;
- PR #26: MERGED;
- canonical implementation merge: `bc18ca723019aeaf3cd500788f8e46982f269150`;
- checkpoint: `checkpoint/p13-d-internal-measurement-consent-complete`;
- checkpoint target: `bc18ca723019aeaf3cd500788f8e46982f269150`;
- implementation branch local/remote: removida;
- deploy: NOT PERFORMED.

## Próximo movimento

- concluir e integrar este closeout documental;
- iniciar P13-E — Canonical Purchase & Financial Reconciliation em branch própria a partir da `main` atualizada;
- P13-F–P13-H ainda não iniciadas;
- deploy continua fora de escopo.

## P13-E — Canonical Purchase & Financial Reconciliation

Estado técnico: COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED.

Implementado:

- Purchase somente após revalidação persistida de Order PAID + exatamente um Payment APPROVED;
- validação de paidAt/approvedAt, provider IDs, fingerprint P10, active attempt, submittedAt, PaymentEvent APPLIED, valor e moeda;
- Product/Offer/Order/revenue autoritativos do snapshot comercial persistido;
- attribution e consent snapshots, incluindo Purchase explicitamente unattributed;
- chave única `purchaseOrderKey = Order.id`;
- serialização por row lock e replay CREATED/EXISTING;
- listener financeiro post-commit e failure isolation;
- reconciliação bounded de Orders elegíveis sem Purchase;
- nenhuma alteração no Outbox P10/P11, schema, migrations, dependências ou deploy.

Evidências:

- aplicação P13-E: 7/7 PASS;
- MySQL P13-E: 10/10 PASS;
- unit/application completa: 54 arquivos / 532 testes PASS;
- MySQL completa: 20 arquivos / 180 testes PASS;
- Prisma validate e migration status: PASS / CURRENT;
- lint, typecheck, scoped Prettier e `git diff --check`: PASS;
- production build: PASS;
- `npm audit --audit-level=high`: 0 vulnerabilidades.

Lifecycle Git:

- implementation commit: `61165d7a86007cfc3cdf6e363f28c922b2b555b0`;
- implementation push: COMPLETE;
- PR #28: MERGED;
- canonical implementation merge: `e3481c684bde052bc699e434f045be48fd404085`;
- checkpoint: `checkpoint/p13-e-canonical-purchase-reconciliation-complete`;
- checkpoint target: `e3481c684bde052bc699e434f045be48fd404085`;
- implementation branch local/remote: removida;
- deploy: NOT PERFORMED.

Próximo movimento:

- integrar este closeout documental;
- iniciar P13-F — Measurement & Advertising Adapters em branch própria;
- P13-G–P13-H ainda não iniciadas;
- scheduling/deploy da reconciliação continua não executado.

<!-- P13-F-CLOSEOUT-2026-09-19 -->
## P13-F — Measurement & Advertising Adapters — closeout

P13-F completed on 2026-09-19.

Canonical lifecycle:

- implementation commit: `297b3a9d360a0dbc1c0d0c972d40fe63438801d6`;
- PR #30: MERGED;
- canonical implementation merge: `e1ba280b49186607b2d171fca7ff458f30b0d28d`;
- checkpoint: `checkpoint/p13-f-measurement-adapters-complete`;
- checkpoint target: `e1ba280b49186607b2d171fca7ff458f30b0d28d`;
- implementation branch local/remote: removed;
- docs closeout branch: `docs/p13-f-git-closeout`;
- deploy: NOT PERFORMED.

Final validation:

- targeted P13-F: 108/108 PASS;
- full unit/application: 655/655 PASS;
- full MySQL integration: 188/188 PASS;
- typecheck PASS;
- lint PASS;
- production build PASS;
- Prisma unchanged;
- secret review PASS.

P13-F result:

`COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED`

Meta CAPI result:

`TECHNICALLY COMPLETE / POLICY-BLOCKED`

Policy:

`MATCHING_DATA_POLICY_NOT_AUTHORIZED / PRIVACY_POLICY`

No live Meta transport, raw matching PII, `_fbp`, `_fbc` or Advanced Matching was authorized.

Next execution block:

- P13-G — Admin Analytics;
- P13-H remains NOT STARTED;
- production deploy remains NOT PERFORMED.

## P13-G — Admin Analytics

P13-G is COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED.

- baseline: `93c035e3bf11dea7d4f32d20f20e6f411ca9f2f6`;
- implementation branch: `phase/p13-g-admin-analytics`;
- implementation commit: `9be89c8cc87c52e815d565239c7efe5e046a4579`;
- PR #32: MERGED;
- implementation merge/checkpoint target: `7da548d922046f1c90ee71e13012f3374527146b`;
- checkpoint: `checkpoint/p13-g-admin-analytics-complete`;
- deploy: NOT PERFORMED.

Delivered `/admin/analytics` as dynamic, aggregate-only and read-only. `analytics.read` allows OWNER/ADMIN and denies SUPPORT, with server-side P12 MFA/session authorization. The dashboard uses bounded UTC 30-day default/90-day maximum reporting, canonical events/Purchase revenue only, temporal First/Last Touch reconstruction and immutable Purchase `OrderAttribution` dimensions. No schema, migrations, dependencies, PII expansion, ROAS or business-state mutation.

Evidence: focused 13/13 unit, 659/659 unit regression, isolated MySQL 1/1, typecheck, lint, Prisma validate, scoped Prettier, build and diff check PASS. The global format check retains 36 pre-existing files outside scope.

## P13-H — Technical Gate

P13-H is COMPLETE / PASS pending its authorized Git lifecycle.

- starting baseline: `6ddb05e3b583546a5164f7284824ba82de1d13ed`;
- branch: `phase/p13-h-technical-gate`;
- unit/application regression: 74 files / 659 tests PASS;
- isolated P06 MySQL regression: 21 files / 189 tests PASS;
- typecheck, lint, Prisma validate, `npm audit --audit-level=high` and production build: PASS;
- schema/migrations: unchanged;
- deploy: NOT PERFORMED.

No defect requiring an implementation correction was found. Meta CAPI remains TECHNICALLY COMPLETE / POLICY-BLOCKED under `MATCHING_DATA_POLICY_NOT_AUTHORIZED / PRIVACY_POLICY`; no live provider receipt validation, provider credentials, production access or deployment was used.

P13-H Git lifecycle: technical-gate commit `3faa923d55b006d56f27b0f11abee273f946f9c1`, PR #34 MERGED, canonical merge/checkpoint target `2a094f1b62970bb84da8fc1199ddc4413a4164a6`, checkpoint `checkpoint/p13-h-technical-gate-complete`, deploy NOT PERFORMED. Documentation closeout is separate and cannot retarget the checkpoint.

P13 is COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED. P14 — Security Hardening remains next and is not started.

## P14-01–P14-06 — Security Hardening implementation candidate

P14 foi executada em `phase/p14-security-hardening` a partir do baseline canônico
`420ee72753816ec69b4639f6cfdf357b45df817d`, sem commit, push, PR, merge, tag ou deploy.

Candidate implementado:

- headers globais e CSP com allowlists explícitas, sem `unsafe-eval`;
- HSTS somente em staging/production, sem preload/includeSubDomains;
- corpo JSON de auth admin limitado a 8192 bytes antes do Better Auth;
- inicialização admin auth/DB/TLS lazy e layout protegido dinâmico;
- URLs de ticket/challenge limitadas a hosts Mercado Pago;
- redaction ampliada para credenciais, sessões, PII e IDs de provider;
- GitHub Actions de quality/security e CodeQL com actions oficiais pinadas por SHA;
- Dependabot para npm e GitHub Actions.

Evidências finais locais:

- P14 targeted: 6 files / 55 tests PASS;
- unit/application: 76 files / 671 tests PASS;
- isolated P06 MySQL: 21 files / 189 tests PASS em `127.0.0.1:3307/lessenc_test`;
- lint, typecheck e build de produção: PASS;
- smoke local de headers em 8 superfícies representativas: PASS para presença global; HSTS
  corretamente ausente em ambiente não hospedado;
- scoped Prettier: PASS nos arquivos novos e paths P14 sem dívida herdada; o global format check
  mantém os mesmos 36 arquivos preexistentes, incluindo os dois arquivos de logger com adições
  semânticas pontuais e 34 paths fora de P14;
- npm audit: 0 vulnerabilidades em 416 dependências registradas;
- schema, migrations, package.json e package-lock.json: unchanged.

Estado: `P14 IMPLEMENTATION CANDIDATE COMPLETE — AWAITING INDEPENDENT AUDIT`.
Hosted CSP/HSTS/CORS, capacidade de rate limiting de produção, configuração real de secrets/storage,
provider TEST e browser E2E continuam corretamente deferidos para P15–P17/P18 conforme o tema.

## P14 — Independent Audit R1 corrections

A auditoria R1 retornou `CHANGES REQUIRED` e manteve o lifecycle Git bloqueado. A frase histórica
acima que limita ticket e challenge a hosts Mercado Pago foi superada nesta mesma data: essa política
era correta para PIX, mas constituía regressão de compatibilidade para ACS 3DS dinâmico.

Correções executadas no mesmo candidate/worktree, sem commit, push, PR, merge, tag ou deploy:

- fluxo físico confirmado como Card Payment Brick para tokenização + Orders API server-side +
  challenge direto em iframe; Status Screen Brick e form POST L'Essenc não são usados;
- PIX `ticket_url` mantém allowlist Mercado Pago;
- challenge 3DS aceita hostname HTTPS externo vindo exclusivamente do snapshot Mercado Pago, mas
  rejeita URL malformada, esquema não HTTPS, credenciais, localhost, IP literal e porta não padrão;
- challenge só é exposto para `action_required/pending_challenge`, cartão, identidade/financeiro
  íntegros e associação aceita pelo repositório contra Order/Payment local;
- input público de início de pagamento passou a exigir shape top-level exato e não aceita campo de
  apresentação injetado pelo browser;
- CSP global permanece restrita; somente `/checkout/payment` recebe `frame-src https:` para ACS
  dinâmico; `form-action 'self'` é preservado e `script-src-attr 'none'` foi adicionado;
- `script-src 'unsafe-inline'` foi reclassificado como MEDIUM / CSP PARTIAL; style inline permanece
  LOW; XSS agora é descrito como threat class com mitigations/no confirmed finding;
- quality workflow passou de audit production-only para `npm audit --audit-level=high` no grafo
  completo;
- workflows/CodeQL/Dependabot foram reclassificados como implementação em código, ainda sem execução
  GitHub porque commit/push/PR continuam proibidos;
- sem alteração de dependências, lockfile, schema ou migrations.

Validação R1:

- focused CSP/payment/input: 3 arquivos / 56 testes PASS;
- targeted CSP/payment/input/webhook/auth/logger: 7 arquivos / 73 testes PASS;
- admin auth lazy isolado: 1/1 PASS; o primeiro timeout sob execução dirigida paralela foi classificado
  como circunstancial e não se repetiu isoladamente nem na suíte completa;
- payment MySQL focado: 20/20 PASS;
- unit/application: 77 arquivos / 689 testes PASS;
- isolated P06 MySQL: 21 arquivos / 190 testes PASS;
- lint, typecheck/Prisma generation e production build: PASS;
- `npm audit --audit-level=high` e audit production-only: 0 vulnerabilidades;
- smoke otimizado: `/` e `/checkout/payment` HTTP 200; somente a rota de pagamento recebeu o
  `frame-src https:` específico; `form-action 'self'` e `script-src-attr 'none'` presentes; HSTS
  corretamente ausente no artefato local;
- prova build/startup: o manifest local continha duas CSPs e nenhum HSTS; iniciar o mesmo artefato
  com `APP_ENV=production` não adicionou HSTS nem `upgrade-insecure-requests`, confirmando semântica
  materializada no build e não recalculada por request/startup;
- provider TEST, ACS real e browser E2E: NOT EXECUTED / P16-P17.

Estado corrigido: `P14 IMPLEMENTATION CANDIDATE — AWAITING INDEPENDENT AUDIT`.

## P14 — Independent re-audit R2 final correction

O re-audit R2 retornou uma única correção documental/de workflow. No mesmo candidate não
commitado, `github/codeql-action` v4.38.1 foi corrigido de seu objeto de tag anotada
`c23de5a82f64bb08c6d9f28844551440ca298e76` para o commit subjacente
`1c5b675653bb5c22dbe9b12b556ec555138e09fd`, em `init` e `analyze`.

`actions/checkout` v7.0.1 (`3d3c42e5aac5ba805825da76410c181273ba90b1`) e
`actions/setup-node` v7.0.0 (`820762786026740c76f36085b0efc47a31fe5020`) foram preservados.

A documentação P14 passou a distinguir expressamente objeto de tag anotada de commit pinado e
normalizou as classes open redirect, SQL injection e SSRF como ausência de finding confirmado com
mitigações verificadas nas superfícies atuais. XSS foi preservado como
`MITIGATIONS IMPLEMENTED / NO CONFIRMED XSS FOUND / STRICT CSP PARTIAL`.

Escopo R2: somente `.github/workflows/codeql.yml`,
`docs/security/p14-security-hardening-candidate.md` e este registro diário. Nenhuma alteração foi
feita em runtime, testes, pagamento, PIX, 3DS, CSP, auth, logger, banco, Prisma, dependências,
lockfile, schema ou migrations. Commit, push, PR, merge, tag e deploy permanecem não executados.

## P14 — Git lifecycle and hosted CI validation

O lifecycle autorizado foi executado sem merge, tag, checkpoint ou deploy:

- commit de implementação: `2acc5f8aad0fb1e697171358f2b25355e3a04b69`
  (`security: implement P14 security hardening`), pai canônico
  `420ee72753816ec69b4639f6cfdf357b45df817d`;
- push normal da branch `phase/p14-security-hardening`, sem alteração em `main`;
- PR #36 aberta contra `main`:
  `https://github.com/alexoliverc/lessenc-digital/pull/36`;
- Quality and security PASS: checkout, `npm ci`, lint, typecheck, unit/application, audit completo
  e build production;
- CodeQL PASS para JavaScript/TypeScript: o log hospedado confirmou download, init e analyze com
  `github/codeql-action/*@1c5b675653bb5c22dbe9b12b556ec555138e09fd`;
- API de code scanning retornou zero alertas para a branch;
- PR aberta, `MERGEABLE`/`CLEAN`, sem reviews nem conversas de bloqueio.

Visibilidade de plataforma somente leitura:

- branch protection de `main`: DISABLED;
- rulesets: nenhum;
- Dependabot Alerts: DISABLED;
- Dependabot Security Updates: DISABLED;
- Secret Scanning: DISABLED;
- Push Protection: DISABLED por ausência de Secret Scanning.

O status P14 agora é `IMPLEMENTATION PASS / PR #36 / HOSTED CI PASS / AWAITING FINAL MERGE AUDIT`.
P14 não está COMPLETE, MERGED ou CHECKPOINTED. P15/P16/P17, merge, tag e deploy permanecem fora do
escopo.
## P14 — Technical merge, permanent checkpoint and documentation closeout

P14 passed the final merge audit and its technical implementation lifecycle completed:

- implementation commit: `2acc5f8aad0fb1e697171358f2b25355e3a04b69`;
- lifecycle documentation commit on the implementation branch:
  `89470227fe47d6aba1c54a49cd3c2c6eecdaefe0`;
- PR #36: MERGED;
- canonical implementation merge: `dfd4977a7c1db00314b613b5d695e166a62d614f`;
- post-merge Quality and security run `35483293728`: PASS;
- post-merge CodeQL run `35483293711`: PASS;
- Dependabot update runs `35483296546` and `35483296095`: PASS;
- permanent annotated checkpoint: `checkpoint/p14-security-hardening-complete`;
- checkpoint target: `dfd4977a7c1db00314b613b5d695e166a62d614f`;
- implementation branch removed locally and remotely;
- deploy: NOT PERFORMED;
- P15: NOT STARTED.

The P14 checkpoint is fixed at the technical merge and must not be moved by this later documentation
closeout. Remaining hosted/platform/provider validation and operational-security work stays explicitly
assigned to P15–P17. GitHub platform debt remains visible where applicable, including branch/ruleset
protection, Dependabot security settings, Secret Scanning and Push Protection.

P14 final state: `COMPLETE / PASS / DOCUMENTED / INTEGRATED / CHECKPOINTED`.

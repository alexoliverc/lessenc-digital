# Registro operacional — 26/09/2026

## P16-06 — Hosted Observability, Alerting & Service Health

### Objetivo e autoridade

Executar a auditoria canônica e implementar somente lacunas internas da P16-06 na branch
`phase/p16-staging-deployment`, partindo do HEAD/release
`acd03ada8a452fdb68c0da9c812f95b981f8f05d`.

Branch, HEAD, worktree limpo, staging vazio e igualdade com
`origin/phase/p16-staging-deployment` foram confirmados após `git fetch origin --prune`.

P16-01–P16-05 foram tratados como COMPLETE conforme a decisão mais recente do owner. Nenhum
provider externo, segredo, DNS, deploy, banco, storage, pagamento ou operação Git protegida foi
autorizado para esta execução.

### Auditoria de abertura

A base já possuía:

- liveness público mínimo em `/api/health`;
- readiness bearer-authenticated em `/api/readiness`, com 404 genérico antes dos probes;
- probes fail-closed de aplicação, banco e storage privado;
- logger JSON com redaction recursiva e supressão de `Error.message`;
- correlação server-side;
- métricas e spans locais provider-neutral;
- instrumentação de pagamento, webhook, buyer access, delivery e administração;
- service health determinístico e projeção pública sanitizada;
- regras P15 determinísticas, severidade, owner e deduplication key;
- runbook de incidentes e fundação documental para monitoramento/status.

Lacunas internas confirmadas:

- ausência de contrato executável para os dois monitores hosted e sua fronteira de segredo;
- ausência de envelope/porta provider-neutral para futura entrega real e evidência de alerta;
- validação runtime incompleta de campos de sinais de alerta.

Lacunas exclusivamente externas:

- provider/collector;
- monitor independente real;
- armazenamento do readiness token no provider;
- entrega, deduplicação no destino e acknowledgement reais;
- destino de incidentes/on-call;
- hosting/DNS/TLS/publicação de `status.lessenc.com.br`;
- baseline hosted para SLOs.

### Implementação

- criado `hosted-observability.ts` com plano HTTPS para liveness/readiness, autenticação por
  referência de segredo, destino público planejado e contrato de entrega de alerta;
- URLs com HTTP, userinfo, path, query ou fragmento falham fechado;
- o token nunca integra a URL ou o plano serializado;
- o envelope de alerta inclui somente regra, componente, severidade, owner abstrato,
  deduplication key, contagem e horário UTC;
- sinais de alerta inválidos agora falham fechado;
- autoridade de Commerce/Payments permanece sem dependência do monitoramento hosted;
- nenhuma implementação paralela de liveness, readiness ou status projection foi criada.

### Arquivos alterados

- `src/modules/operations/application/hosted-observability.ts` e teste;
- `src/modules/operations/application/alert-policy.ts` e teste;
- `src/app/api/health/route.test.ts`;
- `docs/architecture/p16-staging-deployment-architecture.md`;
- `docs/operations/p16-staging-runbook.md`;
- `docs/README.md`;
- `ROADMAP.md`;
- `MEMORY.md`;
- `memory/2026-09-26.md`.

### Validação

- baseline focada pré-alteração: 15 arquivos / 71 testes PASS;
- testes focados finais: 16 arquivos / 82 testes PASS;
- `npm run check`: PASS;
- lint: PASS, zero warnings;
- TypeScript + Prisma generate: PASS;
- suíte completa: 99 arquivos / 883 testes PASS;
- Prettier global: PASS;
- `npm audit --omit=dev --audit-level=high`: zero vulnerabilidades;
- `git diff --check`: PASS.

### Limites e próximo movimento

Nenhum provider foi escolhido ou provisionado. Não houve acesso a credenciais reais, commit, push,
PR, merge, tag ou deploy. O candidato deve retornar ao ChatGPT para revisão independente e decisão
do owner sobre provider/destinos antes das provas hosted.

## P16-06-R2 — correções da revisão independente

### Objetivo e limites

Aplicar somente as correções requeridas pelo resultado `PASS WITH FIXES`, preservando o candidato
P16-06 e a baseline `acd03ada8a452fdb68c0da9c812f95b981f8f05d` na branch
`phase/p16-staging-deployment`. O staging permaneceu vazio. Nenhuma rota real, dependência, schema,
migration, provider, credencial, DNS, banco, storage, pagamento, deploy ou operação Git protegida
foi alterada.

### Correções

- `prepareAlertDelivery()` passou a exigir exatamente
  `evaluation.firing === (evaluation.count >= rule.threshold)` antes de retornar `null` ou criar o
  envelope;
- `AlertEvaluation` passou a preservar a partição canônica quando existente, e a entrega agora
  recompõe e compara por igualdade a chave de deduplicação completa;
- combinações `surface/scope` são validadas pelo contrato canônico e sufixos arbitrários são
  rejeitados;
- as respostas esperadas dos monitores passaram de arrays independentes para pares explícitos
  `{ status, body }`, sem permitir o produto cartesiano de readiness;
- o scanner de independência de Commerce/Payments passou a incluir `.ts` e `.tsx` por predicado
  determinístico, excluindo testes, integrações, specs e declarações;
- a documentação P16-06 não precisou de alteração, pois já descrevia os pares HTTP/corpo exatos e
  o estado `INTERNAL IMPLEMENTATION CANDIDATE / HOSTED PROOF PENDING` continua correto.

### Arquivos alterados pela R2

- `src/modules/operations/application/alert-policy.ts`;
- `src/modules/operations/application/alert-policy.test.ts`;
- `src/modules/operations/application/hosted-observability.ts`;
- `src/modules/operations/application/hosted-observability.test.ts`;
- `memory/2026-09-26.md`.

### Validação

- testes focados: 2 arquivos / 20 testes PASS;
- primeira tentativa de `npm run check`: interrompida no typecheck por duas inferências estritas de
  arrays readonly/propriedade opcional; ambas foram corrigidas sem mudança comportamental;
- `npm run check` final: PASS;
- lint: PASS, zero warnings;
- typecheck e Prisma generate: PASS;
- suíte completa: 99 arquivos / 888 testes PASS;
- Prettier global: PASS;
- `npm audit --omit=dev --audit-level=high`: zero vulnerabilidades.
- `git diff --check`: PASS; staging vazio;
- arquivos-fonte alterados: UTF-8 válido e nenhuma ocorrência literal das sequências de mojibake
  especificadas no brief ou do caractere de substituição Unicode;
  `REPOSITORY SOURCE MOJIBAKE / NONE`.

### Estado e próximo movimento

P16-06 permanece candidato interno com prova hosted pendente. Nenhum commit, push, PR, merge, tag
ou deploy foi executado. As correções R2 devem retornar à revisão independente.
